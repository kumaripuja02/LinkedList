#include<bits/stdc++.h>
using namespace std;
class Node
{
    int data;
    Node* next;
    Node(int data)
    {
        this->data= data;
        this->next= NULL;
    }
};
void sortLL(Node* & head, int data)
{
Node* temp= new Node(data);
}
int main()
{
    Node* NewNode= new Node(data);
    sortLL(head);
    return 0;
}